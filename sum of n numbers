#include<stdio.h>
void main()
{
	int n;
	printf("Enter the no : ");
	scanf("%d",&n);
	int temp =sum(n);
	printf("Sum of %d no is : %d ",n,temp);
}
int sum(n)
{
	int i;
	int sum =0;
	for(i=0;i<=n;i++)
	{
		sum=sum+i;
	}
	return sum;
}
